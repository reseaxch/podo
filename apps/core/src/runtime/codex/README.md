# Codex runtime

Owns Codex app-server process supervision inside core. Protocol transport lives in `@rootline/codex-app-server-client`; this directory maps Codex threads, turns, items, and approvals into Rootline domain state.
